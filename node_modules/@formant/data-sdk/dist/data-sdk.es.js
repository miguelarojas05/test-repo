var ce = Object.defineProperty;
var de = (a, e, t) => e in a ? ce(a, e, { enumerable: !0, configurable: !0, writable: !0, value: t }) : a[e] = t;
var s = (a, e, t) => (de(a, typeof e != "symbol" ? e + "" : e, t), t);
import { decode as V } from "base-64";
import * as u from "date-fns";
import { startOfMinute as he, addMinutes as le, roundToNearestMinutes as ue, addSeconds as me } from "date-fns";
import { RtcClient as A, SignalingPromiseClient as N } from "@formant/realtime-sdk";
import { EventEmitter as fe } from "eventemitter3";
import { deflate as we } from "pako";
import { fromByteArray as pe } from "base64-js";
const ye = "https://api.formant.io";
function ge(a, e) {
  try {
    if (e.get("formant_stage"))
      return "https://api-stage.formant.io";
    if (e.get("formant_dev"))
      return "https://api-dev.formant.io";
    if (e.get("formant_local"))
      return "https://api.formant.local";
    if (e.get("formant_url")) {
      const t = e.get("formant_url");
      if (t !== null)
        try {
          return new URL(t).origin;
        } catch {
          console.warn(
            `Ignoring malformed \`formant_url\` url parameter: ${t}`
          );
        }
    }
  } catch {
  }
  return typeof a < "u" && "FORMANT_API_URL" in a && typeof a.FORMANT_API_URL == "string" ? a.FORMANT_API_URL : ye;
}
const h = ge(
  typeof window < "u" ? window : globalThis,
  new URLSearchParams(
    typeof window < "u" && window.location ? window.location.search : void 0
  )
);
class x extends Error {
  constructor(t) {
    super("login failed");
    s(this, "reason");
    this.reason = t, this.name = "LoginFailureError", Object.setPrototypeOf(this, new.target.prototype);
  }
}
class U extends Error {
  constructor(t) {
    super("login challenged");
    s(this, "challenge");
    this.challenge = t, this.name = "LoginChallengedError", Object.setPrototypeOf(this, new.target.prototype);
  }
}
class ve {
  constructor({
    apiUrl: e,
    refreshAuthToken: t,
    addAccessTokenRefreshListener: n
  }) {
    s(this, "_refreshToken");
    s(this, "_isShareToken", !1);
    s(this, "_currentOrganization");
    s(this, "_currentUser");
    s(this, "_defaultDeviceId");
    s(this, "_token");
    s(this, "_waitingForAuth", /* @__PURE__ */ new Set());
    s(this, "_refreshTimer");
    s(this, "_apiUrl");
    s(this, "_refreshAuthToken");
    s(this, "_addAccessTokenRefreshListener");
    this._apiUrl = e, this._refreshAuthToken = t, this._addAccessTokenRefreshListener = n;
  }
  get token() {
    return this._token;
  }
  get currentUser() {
    return this._currentUser;
  }
  get currentOrganization() {
    return this._currentOrganization;
  }
  get defaultDeviceId() {
    return this._defaultDeviceId;
  }
  /**
   * @deprecated Do not use directly. This will be removed in future versions of the API
   */
  get refreshToken() {
    return this._refreshToken;
  }
  /**
   * @deprecated Do not use directly. This will be removed in future versions of the API
   */
  get isShareToken() {
    return this._isShareToken;
  }
  async login(e, t, n = {}) {
    const { advanced: i = !1 } = n;
    try {
      const o = await fetch(`${this._apiUrl}/v1/admin/auth/login`, {
        method: "POST",
        body: JSON.stringify({ email: e, password: t }),
        headers: {
          "Content-Type": "application/json"
        }
      }), c = await o.json();
      if (o.status !== 200)
        throw new x(c.message);
      if ("challenge" in c)
        throw new U(c.challenge);
      const { authentication: d } = c;
      return await this.loginWithToken(
        d.accessToken,
        d.refreshToken
      ), i ? {
        result: "success",
        authentication: d
      } : d;
    } catch (o) {
      if (i || console.error("login() failed", { err: o }), this._waitingForAuth.forEach((c) => c(!1)), this._waitingForAuth.clear(), !i)
        throw o;
      return o instanceof U ? {
        result: "challenged",
        challenge: o.challenge
      } : {
        result: "failure",
        reason: o instanceof x ? o.reason : o instanceof Error ? o.message : String(o)
      };
    }
  }
  async loginWithToken(e, t) {
    var i;
    const n = JSON.parse(V(e.split(".")[1]));
    try {
      let o;
      if (this._isShareToken = n["formant:claims"] && n["formant:claims"].type == "share", n["formant:claims"] && (this._currentOrganization = n["formant:claims"].organizationId), n["custom:organization_id"] && (this._currentOrganization = n["custom:organization_id"]), this._isShareToken || (o = n.sub), n["formant:claims"] && n["formant:claims"].userId && (o = n["formant:claims"].userId), o && ((i = this._currentUser) == null ? void 0 : i.id) !== o) {
        const c = await fetch(`${this._apiUrl}/v1/admin/users/${o}`, {
          method: "GET",
          headers: {
            "Content-Type": "application/json",
            Authorization: "Bearer " + e
          }
        }), d = await c.json();
        if (c.status !== 200)
          throw new Error(d.message);
        this._currentUser = d;
      }
      this._token = e, this._waitingForAuth.forEach((c) => c(!0));
    } catch (o) {
      console.error("loginWithToken() failed", { err: o }), this._waitingForAuth.forEach((c) => c(!1));
    } finally {
      this._waitingForAuth.clear();
    }
    t && (this._refreshToken = t, setInterval(async () => {
      if (this._refreshToken) {
        const c = await (await fetch(`${this._apiUrl}/v1/admin/auth/refresh`, {
          method: "POST",
          headers: {
            "Content-Type": "application/json"
          },
          body: JSON.stringify({
            refreshToken: this._refreshToken
          })
        })).json();
        this._token = c.authentication.accessToken;
      }
    }, 1e3 * 60 * 60));
  }
  isAuthenticated() {
    return this._token !== void 0;
  }
  /**
   * @deprecated use currentUser property instead.
   */
  getCurrentUser() {
    return this._currentUser;
  }
  async waitTilAuthenticated() {
    return this.token !== void 0 ? !0 : new Promise((e) => {
      this._waitingForAuth.add(e);
    });
  }
  async listenForRefresh() {
    const t = () => {
      this._refreshTimer = void 0, this._refreshAuthToken();
    };
    this._addAccessTokenRefreshListener((n) => {
      this._refreshTimer && clearTimeout(this._refreshTimer), this._refreshTimer = setTimeout(t, 36e5), this.loginWithToken(n);
    }), this._refreshTimer = setTimeout(t, 36e5);
  }
  async forgotPassword(e) {
    await fetch(`${this._apiUrl}/v1/admin/auth/forgot-password`, {
      method: "POST",
      body: JSON.stringify({ email: e }),
      headers: {
        "Content-Type": "application/json"
      }
    });
  }
  /**
   * @example
   * // Body
   * await this.confirmForgotPassword({
   *     email: "joe@gmail.com"
   *     confirmationCode: "1",
   *     newPassword: "NewPassword"
   *   });
   */
  async confirmForgotPassword(e) {
    return (await fetch(
      `${this._apiUrl}/v1/admin/auth/confirm-forgot-password`,
      {
        method: "POST",
        body: JSON.stringify(e),
        headers: {
          "Content-Type": "application/json"
        }
      }
    )).ok;
  }
  async respondToNewPasswordRequiredChallenge(e) {
    const t = await fetch(
      `${this._apiUrl}/v1/admin/auth/respond-to-new-password-required-challenge`,
      {
        method: "POST",
        body: JSON.stringify(e),
        headers: {
          "Content-Type": "application/json"
        }
      }
    );
    if (t.ok)
      return await t.json();
    throw new Error("respond-to-new-password-required-challenge failed");
  }
  async loginWithGoogle(e) {
    return await (await fetch(`${this._apiUrl}/v1/admin/auth/login-google`, {
      method: "POST",
      body: JSON.stringify(e),
      headers: {
        "Content-Type": "application/json"
      }
    })).json();
  }
  async refresh(e) {
    const n = await (await fetch(`${this._apiUrl}/v1/admin/auth/refresh`, {
      method: "POST",
      headers: {
        "Content-Type": "application/json"
      },
      body: JSON.stringify({
        refreshToken: e
      })
    })).json();
    await this.loginWithToken(n.authentication.accessToken, e);
  }
  async checkSso(e, t) {
    return await (await fetch(`${this._apiUrl}/v1/admin/auth/check-sso`, {
      method: "POST",
      body: JSON.stringify({ email: e, allowUserAutoCreation: t }),
      headers: {
        "Content-Type": "application/json"
      }
    })).json();
  }
  async loginWithSso(e, t) {
    const i = await (await fetch(`${this._apiUrl}/v1/admin/auth/login-sso`, {
      method: "POST",
      body: JSON.stringify({ token: e, refreshToken: t }),
      headers: {
        "Content-Type": "application/json"
      }
    })).json();
    if (!i.authentication)
      throw new Error("Failed to login with SSO");
    return await this.loginWithToken(
      i.authentication.accessToken,
      i.authentication.refreshToken
    );
  }
}
function k() {
  return typeof window < "u" && window.location ? new URLSearchParams(window.location.search).get("module") : null;
}
function T(a) {
  if (!(window && window.parent))
    throw new Error("cannot send message to non-existent parent");
  window.parent.postMessage(a, "*");
}
function F() {
  const a = k();
  if (!a)
    throw new Error("No module context");
  T({
    type: "refresh_auth_token",
    module: a
  });
}
function H(a) {
  function e(t) {
    const n = t.data;
    n.type === "auth_token" && a(n.token);
  }
  return window.addEventListener("message", e), () => {
    window.removeEventListener("message", e);
  };
}
const r = new ve({
  apiUrl: h,
  refreshAuthToken: F,
  addAccessTokenRefreshListener: H
});
async function Te(a) {
  return (await (await fetch(
    `${h}/v1/admin/module-configurations/${a}`,
    {
      headers: {
        "Content-Type": "application/json",
        Authorization: "Bearer " + r.token
      }
    }
  )).json()).configuration;
}
function Ce() {
  T({
    type: "hide_analytics_date_picker"
  });
}
function Se(a) {
  T({
    type: "go_to_device",
    deviceId: a
  });
}
function Ee(a) {
  T({
    type: "go_to_time",
    time: a.getTime()
  });
}
function ke() {
  const a = k();
  if (!a)
    throw new Error("No module context");
  T({
    type: "request_module_data",
    module: a
  });
}
function Oe(a, e) {
  const t = k();
  if (!t)
    throw new Error("No module context");
  T({
    type: "send_channel_data",
    source: t,
    channel: a,
    data: e
  });
}
function Re(a, e) {
  const t = k();
  if (!t)
    throw new Error("No module context");
  T({
    type: "set_module_data_time_range",
    module: t,
    before: a,
    after: e || 0
  });
}
function Ae(a) {
  const e = k();
  if (!e)
    throw new Error("No module context");
  T({
    type: "setup_module_menus",
    module: e,
    menus: a
  });
}
function be(a) {
  T({ type: "show_message", message: a });
}
function je(a, e) {
  const t = (n) => {
    const i = n.data;
    i.type === "channel_data" && i.channel === a && e({
      source: i.source,
      data: i.data
    });
  };
  return window.addEventListener("message", t), () => window.removeEventListener("message", t);
}
function Ne(a) {
  const e = (t) => {
    const n = t.data;
    n.type === "module_menu_item_clicked" && a(n.menu);
  };
  return window.addEventListener("message", e), () => window.removeEventListener("message", e);
}
function De(a) {
  const e = (t) => {
    const n = t.data;
    n.type === "module_configuration" && a(n);
  };
  return window.addEventListener("message", e), () => window.removeEventListener("message", e);
}
function Pe(a) {
  const e = k();
  e && T({ type: "request_module_data", module: e });
  const t = (n) => {
    const i = n.data;
    i.type === "module_data" && a({
      streams: i.streams,
      time: i.time,
      queryRange: i.queryRange
    });
  };
  return window.addEventListener("message", t), () => window.removeEventListener("message", t);
}
function Le(a) {
  T({ type: "request_devices" });
  const e = (t) => {
    const n = t.data;
    n.type === "overview_devices" && a(n.data);
  };
  return window.addEventListener("message", e), () => window.removeEventListener("message", e);
}
const Ie = 1, W = 1e3, K = 60 * W, Q = 60 * K, L = 24 * Q, _e = 7 * L, $e = 30 * L, ze = 365 * L, I = {
  millisecond: Ie,
  second: W,
  minute: K,
  hour: Q,
  day: L,
  week: _e,
  month: $e,
  year: ze
};
function Me(a, e) {
  return a.filter((t) => e.includes(t.type));
}
function Be(a, e, t) {
  const n = e.getTime(), i = t.getTime();
  return a.map((o) => ({
    ...o,
    points: o.points.filter(
      ([c]) => c >= n && c < i
    )
  })).filter(({ points: o }) => o.length > 0);
}
class J {
  constructor({
    capacity: e,
    timeout: t
  } = {}) {
    s(this, "entries", /* @__PURE__ */ new Map());
    s(this, "metadata", /* @__PURE__ */ new Map());
    s(this, "capacity");
    s(this, "staleIntervalMs");
    this.capacity = e || 1e4, this.staleIntervalMs = t || I.minute;
  }
  get(e, t) {
    const n = this.keyToCacheKey(e);
    return this.isStale(n) && !this.isGenerating(n) && t && this.generate(e, t), this.entries.get(n);
  }
  set(e, t) {
    const n = this.keyToCacheKey(e);
    this.metadata.set(n, {
      generating: !1,
      staleAt: performance.now() + this.staleIntervalMs
    });
    const i = this.entries.get(n);
    JSON.stringify(i) === JSON.stringify(t) || (this.entries.set(n, t), this.enforceMaxSize());
  }
  clear() {
    this.entries.clear(), [...this.metadata.values()].forEach((e) => e.generating = !1);
  }
  clearKey(e) {
    this.metadata.delete(e), this.entries.delete(e);
  }
  keyToCacheKey(e) {
    return JSON.stringify(e);
  }
  enforceMaxSize() {
    for (; this.metadata.size > this.capacity && this.metadata.size > 0; ) {
      const [e] = [...this.metadata.entries()].reduce(
        ([t, n], [i, o]) => o.staleAt < n.staleAt ? [i, o] : [t, n]
      );
      this.clearKey(e);
    }
  }
  isStale(e) {
    const t = this.metadata.get(e);
    return t ? (t == null ? void 0 : t.staleAt) < performance.now() : !0;
  }
  isGenerating(e) {
    const t = this.metadata.get(e);
    return t ? t.generating : !1;
  }
  generate(e, t) {
    const n = this.keyToCacheKey(e), i = this.metadata.get(n) || {}, o = t().then((c) => {
      const d = this.metadata.get(n);
      return (d == null ? void 0 : d.generating) !== o || this.set(e, c), c;
    }).catch((c) => {
      throw this.metadata.delete(n), c;
    });
    this.metadata.set(n, {
      ...i,
      generating: o,
      staleAt: performance.now() + this.staleIntervalMs
    });
  }
}
async function _(a) {
  if (!r.token)
    throw new Error("Not authenticated");
  return (await (await fetch(`${h}/v1/queries/queries`, {
    method: "POST",
    body: JSON.stringify(a),
    headers: {
      "Content-Type": "application/json",
      Authorization: "Bearer " + r.token
    }
  })).json()).items;
}
class qe {
  constructor() {
    s(this, "queryStoreCache", new J({
      capacity: 1e4,
      timeout: 20 * I.second
    }));
    s(this, "liveQueryStoreCache", new J({
      capacity: 1e4,
      timeout: 200 * I.millisecond
    }));
  }
  moduleQuery(e, t, n, i, o, c = !1) {
    const d = {
      ...e,
      names: [...t],
      types: [...n]
    }, y = this.query(d, i, o, c);
    return y === void 0 || y === "too much data" ? y : Me(y, n);
  }
  query(e, t, n, i = !1) {
    const o = {
      ...e,
      start: he(t).toISOString(),
      end: i ? n.toISOString() : le(ue(n), 1).toISOString(),
      latestOnly: i
    }, c = n > me(/* @__PURE__ */ new Date(), -20);
    let d;
    return c ? d = this.liveQueryCache(o) : d = this.queryCache(o), !d || d === "too much data" || i ? d : Be(d, t, n);
  }
  queryCache(e) {
    return this.queryStoreCache.get(e, async () => {
      try {
        return await _(e);
      } catch (t) {
        throw t;
      }
    });
  }
  liveQueryCache(e) {
    return this.liveQueryStoreCache.get(e, async () => {
      try {
        return await _(e);
      } catch (t) {
        throw t;
      }
    });
  }
}
const xe = new qe();
function Ue(a, e, t) {
  const n = (i) => {
    const o = i.data;
    if (o.type === "module_data") {
      const { start: c, end: d } = o.queryRange;
      t(
        xe.moduleQuery(
          {},
          a,
          e,
          new Date(c),
          new Date(d),
          !1
        )
      );
    }
  };
  return window.addEventListener("message", n), () => window.removeEventListener("message", n);
}
async function Je(a, e, t) {
  return new Promise((n) => {
    T({
      type: "request_date",
      minTime: e,
      maxTime: t,
      time: a
    });
    const i = (o) => {
      const c = o.data;
      c.type === "date_response" && (window.removeEventListener("message", i), n(c.data));
    };
    window.addEventListener("message", i);
  });
}
async function Ge(a, e) {
  return new Promise((t) => {
    const n = Math.random().toString();
    T({
      type: "prompt",
      promptId: n,
      schema: a,
      okText: e == null ? void 0 : e.okText,
      cancelText: e == null ? void 0 : e.cancelText
    });
    const i = (o) => {
      const c = o.data;
      c.type === "prompt_response" && c.promptId === n && t(c.data), window.removeEventListener("message", i);
    };
    window.addEventListener("message", i);
  });
}
const q = class {
  static isModule() {
    return k() !== null;
  }
  static async getCurrentModuleConfiguration() {
    let e = new URLSearchParams("");
    typeof window < "u" && window.location && (e = new URLSearchParams(window.location.search));
    const t = e.get("configuration");
    if (!(t === null || t.trim() === ""))
      return Te(t.trim());
  }
  static get isOnline() {
    return q._isOnline;
  }
  static listenForConnectionEvents() {
    const e = (t) => {
      const { data: n } = t;
      n.type === "formant_online" && (this._isOnline = n.online);
    };
    return window.addEventListener("message", e), () => window.removeEventListener("message", e);
  }
  static checkConnection(e = 1e3) {
    return new Promise((t, n) => {
      const i = setTimeout(
        () => n(new Error("deadline expired: took too long")),
        e
      ), o = (c) => {
        window.removeEventListener("message", o), clearTimeout(i);
        const { data: d } = c;
        d.type === "formant_online" && (this._isOnline = d.online, t(d.online));
      };
      window.addEventListener("message", o), T({ type: "formant_online" });
    });
  }
  static waitForConnection(e = 5e3) {
    let t = !1;
    const n = new Promise((c, d) => {
      setTimeout(() => {
        t = !0, d(new Error("deadline expired: took too long"));
      }, e);
    }), i = (c) => new Promise((d) => setTimeout(d, c)), o = async () => {
      for (await i(50); !t && !(this.isOnline || await this.checkConnection); )
        await i(500);
    };
    return Promise.race([n, o()]);
  }
};
let g = q;
s(g, "getCurrentModuleContext", k), // senders
s(g, "disableAnalyticsBottomBar", Ce), s(g, "goToDevice", Se), s(g, "goToTime", Ee), s(g, "refreshAuthToken", F), s(g, "requestModuleData", ke), s(g, "sendChannelData", Oe), s(g, "setModuleDateTimeRange", Re), s(g, "setupModuleMenus", Ae), s(g, "showMessage", be), // listeners
s(g, "addAccessTokenRefreshListener", H), s(g, "addChannelDataListener", je), s(g, "addMenuListener", Ne), s(g, "addModuleConfigurationListener", De), s(g, "addModuleDataListener", Pe), s(g, "addOverviewDeviceListener", Le), s(g, "addStreamListener", Ue), // bidirectional
s(g, "getDate", Je), s(g, "prompt", Ge), s(g, "_isOnline", null);
function f(a, e) {
  if (a !== void 0)
    return a;
  throw new Error(e || "Value is undefined");
}
const p = {
  UNKNOWN: 0,
  TELEOP: 1,
  PORT_FORWARD: 2,
  OBSERVE: 3,
  HEADLESS: 4
}, Zt = {
  ...p,
  Unknown: p.UNKNOWN,
  Teleop: p.TELEOP,
  PortForward: p.PORT_FORWARD,
  Observe: p.OBSERVE,
  Headless: p.HEADLESS,
  unknown: p.UNKNOWN,
  teleop: p.TELEOP,
  portForward: p.PORT_FORWARD,
  observe: p.OBSERVE,
  headless: p.HEADLESS
}, O = Symbol("RtcClientPool.instance");
var Vt;
class D {
  constructor(e) {
    s(this, Vt, null);
    s(this, "createClient");
    s(this, "ttlMs");
    s(this, "proxyHandler");
    s(this, "proxyReceivers", /* @__PURE__ */ new Map());
    s(this, "teardownTimeout", null);
    s(this, "dispatch", (e, t) => {
      this.proxyReceivers.forEach((n) => n == null ? void 0 : n(e, t));
    });
    const { createClient: t, ttlMs: n = 0 } = e;
    this.createClient = t, this.ttlMs = Math.max(n, 0), this.proxyHandler = {
      get: (i, o, c) => {
        switch (o) {
          case "shutdown":
            return () => this.releaseInstance(c);
          default:
            return Reflect.get(i, o, c);
        }
      }
    };
  }
  get isActive() {
    return this[O] !== null;
  }
  get size() {
    return this.proxyReceivers.size;
  }
  get(e) {
    const t = new Proxy(this.allocate(), this.proxyHandler);
    return this.proxyReceivers.set(t, e ?? null), t;
  }
  allocate() {
    if (this[O])
      return this.teardownTimeout && (clearTimeout(this.teardownTimeout), this.teardownTimeout = null), this[O];
    const e = this.createClient(this.dispatch);
    return this[O] = e, e;
  }
  async teardown() {
    const e = this[O];
    if (!e) {
      console.warn("singleton has already been shutdown!");
      return;
    }
    try {
      await e.shutdown();
    } finally {
      this[O] = null;
    }
  }
  async releaseInstance(e) {
    return this.proxyReceivers.delete(e) ? this.proxyReceivers.size !== 0 ? !1 : (!this.teardownTimeout && Number.isFinite(this.ttlMs) && (this.ttlMs === 0 ? await this.teardown() : this.teardownTimeout = setTimeout(() => {
      this.teardown().catch((t) => console.error("teardown failed", { err: t })).finally(() => this.teardownTimeout = null);
    }, this.ttlMs)), !0) : (console.warn("this instance has already been released!"), !1);
  }
}
Vt = O;
const P = async () => f(r.token, "Realtime when user isn't authorized"), R = {
  [p.UNKNOWN]: new D({
    ttlMs: 2500,
    createClient: (a) => new A({
      signalingClient: new N(h),
      getToken: P,
      sessionType: p.UNKNOWN,
      receive: a
    })
  }),
  [p.TELEOP]: new D({
    ttlMs: 2500,
    createClient: (a) => new A({
      signalingClient: new N(h),
      getToken: P,
      sessionType: p.TELEOP,
      receive: a
    })
  }),
  [p.PORT_FORWARD]: new D({
    ttlMs: 2500,
    createClient: (a) => new A({
      signalingClient: new N(h),
      getToken: P,
      sessionType: p.PORT_FORWARD,
      receive: a
    })
  }),
  [p.OBSERVE]: new D({
    ttlMs: 2500,
    createClient: (a) => new A({
      signalingClient: new N(h),
      getToken: P,
      sessionType: p.OBSERVE,
      receive: a
    })
  }),
  [p.HEADLESS]: new D({
    ttlMs: 2500,
    createClient: (a) => new A({
      signalingClient: new N(h),
      getToken: P,
      sessionType: p.HEADLESS,
      receive: a
    })
  })
}, Ve = {
  ...R,
  unknown: R[p.UNKNOWN],
  teleop: R[p.TELEOP],
  portForward: R[p.PORT_FORWARD],
  observe: R[p.OBSERVE],
  headless: R[p.HEADLESS]
}, $ = R[p.TELEOP], Fe = (a) => {
  const { sessionType: e } = a;
  return e ? Ve[e] : $;
};
class He {
  constructor(e) {
    s(this, "token");
    this.captureSession = e;
  }
  async ingestJSON(e) {
    if (!this.token) {
      const n = await (await fetch(
        `${h}/v1/admin/capture-sessions/${this.captureSession.code}/authenticate`,
        {
          method: "POST"
        }
      )).json();
      this.token = n.token;
    }
    await fetch(`${h}/v1/ingest`, {
      method: "POST",
      body: JSON.stringify({
        deviceId: this.captureSession.deviceId,
        name: this.captureSession.streamName,
        type: "json",
        points: [[Date.now(), JSON.stringify(e)]]
      }),
      headers: {
        "Content-Type": "application/json",
        Authorization: "Bearer " + this.token
      }
    });
  }
}
function b(a) {
  return new Promise((e) => setTimeout(e, a));
}
const We = (a) => a !== void 0 && a.capabilities !== void 0 && a.capabilitySet !== void 0;
class Ke {
  constructor(e) {
    s(this, "ready", !1);
    s(this, "listeners", []);
    s(this, "openListeners", []);
    s(this, "closeListeners", []);
    s(this, "errorListeners", []);
    s(this, "binaryListeners", []);
    s(this, "error");
    s(this, "decoder", new TextDecoder());
    this.dataChannel = e, this.dataChannel.binaryType = "arraybuffer", this.dataChannel.onopen = () => {
      this.setReady();
    }, this.dataChannel.onclose = () => {
      this.ready = !1, this.closeListeners.forEach((t) => t());
    }, this.dataChannel.onerror = (t) => {
      console.error(t), this.error = "An error occurred in DataChannel", this.errorListeners.forEach((n) => n(t));
    }, this.dataChannel.onmessage = (t) => {
      this.listeners.forEach((n) => {
        const i = new Uint8Array(t.data), o = this.decoder.decode(i);
        n(o);
      }), this.binaryListeners.forEach((n) => {
        n(new Uint8Array(t.data));
      });
    };
  }
  setReady() {
    this.ready = !0, this.openListeners.forEach((e) => e());
  }
  addOpenListener(e) {
    this.openListeners.push(e);
  }
  removeOpenListener(e) {
    this.openListeners = this.openListeners.filter((t) => t !== e);
  }
  addCloseListener(e) {
    this.closeListeners.push(e);
  }
  removeCloseListener(e) {
    this.closeListeners = this.closeListeners.filter((t) => t !== e);
  }
  addErrorListener(e) {
    this.errorListeners.push(e);
  }
  removeErrorListener(e) {
    this.errorListeners = this.errorListeners.filter((t) => t !== e);
  }
  async waitTilReady() {
    return this.ready ? !0 : new Promise((t, n) => {
      let i = setInterval(() => {
        this.dataChannel.readyState === "open" && this.setReady(), this.ready && (clearInterval(i), t(!0)), this.error && n(this.error);
      }, 10);
    });
  }
  send(e) {
    if (!this.ready)
      throw new Error("Connection has been closed");
    this.dataChannel.send(e);
  }
  sendBinary(e) {
    if (!this.ready)
      throw new Error("Connection has been closed");
    this.dataChannel.send(e);
  }
  addListener(e) {
    this.listeners.push(e);
  }
  removeListener(e) {
    const t = this.listeners.indexOf(e);
    if (t === -1)
      throw new Error("Could not find data channel listener to remove");
    if (this.error)
      throw new Error(this.error);
    this.listeners.splice(t, 1);
  }
  addBinaryListener(e) {
    this.binaryListeners.push(e);
  }
  removeBinaryListener(e) {
    const t = this.binaryListeners.indexOf(e);
    if (t === -1)
      throw new Error("Could not find data channel listener to remove");
    if (this.error)
      throw new Error(this.error);
    this.binaryListeners.splice(t, 1);
  }
}
class Qe {
  constructor(e, t) {
    s(this, "currentListeners", []);
    s(this, "onRealtimeMessage", (e, t) => {
      t.payload.jointState && this.currentListeners.forEach((n) => {
        t.payload.jointState && n(t.payload.jointState);
      });
    });
    this.device = e, this.config = t;
  }
  async synchronize() {
    this.device.addRealtimeListener(this.onRealtimeMessage), this.device.startListeningToRealtimeDataStream(
      this.config.currentJointStateStream
    );
  }
  async desynchronize() {
    this.device.removeRealtimeListener(this.onRealtimeMessage), this.device.stopListeningToRealtimeDataStream(
      this.config.currentJointStateStream
    );
  }
  async addCurrentJointStateListener(e) {
    this.currentListeners.push(e);
  }
}
class X {
  constructor(e, t, n) {
    s(this, "channel");
    s(this, "requestIdToResponseMap", /* @__PURE__ */ new Map());
    this.device = e, this.channel_name = t, this.timeout = n;
  }
  addOpenListener(e) {
    f(this.channel, "channel not initalized").addOpenListener(e);
  }
  removeOpenListener(e) {
    f(this.channel, "channel not initalized").removeOpenListener(
      e
    );
  }
  addCloseListener(e) {
    f(this.channel, "channel not initalized").addCloseListener(e);
  }
  removeCloseListener(e) {
    f(this.channel, "channel not initalized").removeCloseListener(
      e
    );
  }
  addErrorListener(e) {
    f(this.channel, "channel not initalized").addErrorListener(e);
  }
  removeErrorListener(e) {
    f(this.channel, "channel not initalized").removeErrorListener(
      e
    );
  }
}
class Xe extends X {
  constructor() {
    super(...arguments);
    s(this, "RESPONSE_SUCCESS_BYTE", 0);
    s(this, "decoder", new TextDecoder());
  }
  /*
      Request binary payload layout:
          16-bytes         arbitrary-length
      [     ID       ] [        PAYLOAD         ]
  
      Response binary payload layout:
              1-byte               16-bytes         arbitrary-length
      [ SUCCESS OR ERROR BYTE ] [     ID       ] [        PAYLOAD         ]
      */
  generateBinaryId() {
    const t = new Uint8Array(16);
    for (let n = 0; n < t.length; n++)
      t[n] = Math.floor(Math.random() * 256);
    return t;
  }
  async initialize() {
    this.channel = await this.device.createCustomDataChannel(this.channel_name), this.channel.addBinaryListener((t) => {
      const i = t.slice(0, 16).toString();
      if (i.length === 0)
        throw new Error("Invalid response");
      const o = t.slice(16);
      if (o.length === 0)
        throw new Error("Invalid response");
      this.requestIdToResponseMap.has(i) && this.requestIdToResponseMap.set(i, o);
    });
  }
  async request(t) {
    if (this.channel || await this.initialize(), !this.channel)
      throw new Error("Failed to create channel");
    const { channel: n, requestIdToResponseMap: i, timeout: o } = this;
    await n.waitTilReady();
    const c = this.generateBinaryId(), d = c.toString();
    i.set(d, !0), n.sendBinary(new Uint8Array([...c, ...t]));
    const y = (/* @__PURE__ */ new Date()).getTime();
    for (; (/* @__PURE__ */ new Date()).getTime() < y + o; )
      if (await b(50), i.has(d)) {
        const m = i.get(d);
        if (m !== !0) {
          i.delete(d);
          const w = m[0] === this.RESPONSE_SUCCESS_BYTE, v = m.slice(1);
          if (w)
            return v;
          throw console.error({
            name: "AdapterError",
            message: this.decoder.decode(v)
          }), new Error("Binary request datachannel adapter error");
        }
      }
    throw i.delete(d), console.error({
      name: "TimeoutError",
      message: `Request timed out after ${o / 1e3} seconds`
    }), new Error("Binary request data channel request timed out");
  }
}
class Ye extends X {
  generateTextId() {
    return Math.random().toString(36).substring(2) + "-" + Math.random().toString(36).substring(2);
  }
  async initialize() {
    this.channel = await this.device.createCustomDataChannel(this.channel_name), this.channel.addListener((e) => {
      const t = JSON.parse(e), { id: n, data: i, error: o } = t;
      if (!n)
        throw new Error("Invalid response");
      if (!i && !o)
        throw new Error("Invalid response");
      this.requestIdToResponseMap.has(n) && this.requestIdToResponseMap.set(n, t);
    });
  }
  async request(e) {
    if (this.channel || await this.initialize(), !this.channel)
      throw new Error("Failed to create channel");
    const { channel: t, requestIdToResponseMap: n, timeout: i } = this;
    await t.waitTilReady();
    const o = this.generateTextId();
    n.set(o, !0), t.send(
      JSON.stringify({
        id: o,
        data: e
      })
    );
    const c = (/* @__PURE__ */ new Date()).getTime();
    for (; (/* @__PURE__ */ new Date()).getTime() < c + i; )
      if (await b(50), n.has(o)) {
        const d = n.get(o);
        if (d !== !0) {
          n.delete(o);
          const { data: y, error: m } = d;
          if (y)
            return y;
          if (m)
            throw console.error({
              name: "AdapterError",
              message: m
            }), new Error("Text request datachannel adapter error");
        }
      }
    throw n.delete(o), console.error({
      name: "TimeoutError",
      message: `Request timed out after ${i / 1e3} seconds`
    }), new Error("Text request datachannel request timed out");
  }
}
class Y extends fe {
  constructor() {
    super(...arguments);
    s(this, "rtcClient");
    s(this, "remoteDevicePeerId", null);
    s(this, "realtimeListeners", []);
    s(this, "connectionMonitorInterval");
    s(this, "handleMessage", (t, n) => {
      this.realtimeListeners.forEach((i) => i(t, n));
    });
  }
  stopConnectionMonitoring() {
    clearInterval(this.connectionMonitorInterval), this.connectionMonitorInterval = void 0;
  }
  assertNotCancelled(t) {
    if (t)
      throw new Error("Cancelled by deadline");
  }
  getRealtimeStatus() {
    if (this.rtcClient && this.remoteDevicePeerId)
      return this.rtcClient.getConnectionStatus(this.remoteDevicePeerId);
    throw new Error("Realtime connection hasn't been started");
  }
  getRealtimePing() {
    if (this.rtcClient && this.remoteDevicePeerId)
      return this.rtcClient.getPing(this.remoteDevicePeerId);
    throw new Error("Realtime connection hasn't been started");
  }
  addRealtimeListener(t) {
    this.realtimeListeners.push(t);
  }
  removeRealtimeListener(t) {
    const n = this.realtimeListeners.indexOf(t);
    if (n === -1)
      throw new Error("Could not find realtime listener to remove");
    this.realtimeListeners.splice(n, 1);
  }
  async getRealtimeManipulators() {
    const t = await this.getConfiguration(), n = [];
    for (const i of t.teleop.rosStreams ?? [])
      i.topicType == "sensor_msgs/JointState" && n.push(
        new Qe(this, {
          currentJointStateStream: { name: i.topicName },
          plannedJointStateStream: i.plannedTopic ? { name: i.plannedTopic } : void 0,
          planValidStream: i.planValidTopic ? { name: i.planValidTopic } : void 0,
          endEffectorStream: i.endEffectorTopic ? { name: i.endEffectorTopic } : void 0,
          endEffectorLinkName: i.endEffectorLinkName,
          baseReferenceFrame: i.baseReferenceFrame,
          localFrame: i.localFrame
        })
      );
    return n;
  }
  async getRealtimeVideoStreams() {
    var i, o, c;
    const t = await this.getConfiguration(), n = [];
    for (const d of ((i = t.teleop) == null ? void 0 : i.hardwareStreams) ?? [])
      d.rtcStreamType === "h264-video-frame" && n.push({
        name: d.name
      });
    for (const d of ((o = t.teleop) == null ? void 0 : o.rosStreams) ?? [])
      d.topicType == "formant/H264VideoFrame" && n.push({
        name: d.topicName
      }), (d.topicType === "sensor_msgs/Image" || d.topicType === "sensor_msgs/CompressedImage") && d.encodeVideo && n.push({
        name: d.topicName
      });
    for (const d of ((c = t.teleop) == null ? void 0 : c.customStreams) ?? [])
      d.rtcStreamType === "h264-video-frame" && n.push({
        name: d.name
      });
    return n;
  }
  createCustomRequestDataChannel(t, n = 3e3) {
    return new Ye(this, t, n);
  }
  createCustomBinaryRequestDataChannel(t, n = 3e3) {
    return new Xe(this, t, n);
  }
  async startListeningToRealtimeVideo(t) {
    const n = f(
      this.rtcClient,
      "Realtime connection has not been started"
    ), i = await this.getRemotePeer();
    n.controlRemoteStream(f(i).id, {
      streamName: t.name,
      enable: !0,
      pipeline: "rtc"
    });
  }
  async stopListeningToRealtimeVideo(t) {
    const n = f(
      this.rtcClient,
      "Realtime connection has not been started"
    ), i = await this.getRemotePeer();
    n.controlRemoteStream(f(i).id, {
      streamName: t.name,
      enable: !1,
      pipeline: "rtc"
    });
  }
  async startListeningToRealtimeDataStream(t) {
    const n = f(
      this.rtcClient,
      "Realtime connection has not been started"
    ), i = await this.getRemotePeer();
    n.controlRemoteStream(f(i).id, {
      streamName: t.name,
      enable: !0,
      pipeline: "rtc"
    });
  }
  async stopListeningToRealtimeDataStream(t) {
    const n = f(
      this.rtcClient,
      "Realtime connection has not been started"
    ), i = await this.getRemotePeer();
    n.controlRemoteStream(f(i).id, {
      streamName: t.name,
      enable: !1,
      pipeline: "rtc"
    });
  }
  async enableRealtimeTelemetryPriorityIngestion(t) {
    const n = f(
      this.rtcClient,
      "Realtime connection has not been started"
    ), i = await this.getRemotePeer();
    n.controlRemoteStream(f(i).id, {
      streamName: t,
      enablePriorityUpload: !0,
      pipeline: "telemetry"
    });
  }
  async disableRealtimeTelemetryPriorityIngestion(t) {
    const n = f(
      this.rtcClient,
      "Realtime connection has not been started"
    ), i = await this.getRemotePeer();
    n.controlRemoteStream(f(i).id, {
      streamName: t,
      enablePriorityUpload: !1,
      pipeline: "telemetry"
    });
  }
  async changeStreamAudioType(t, n) {
    const i = f(
      this.rtcClient,
      "Realtime connection has not been started"
    ), o = await this.getRemotePeer();
    i.controlRemoteStream(f(o).id, {
      streamName: t,
      setAudioFormat: n
    });
  }
  async createCustomDataChannel(t, n) {
    const i = f(
      this.rtcClient,
      "Realtime connection has not been started"
    ), o = await this.getRemotePeer(), c = await new Promise((d) => {
      i.createCustomDataChannel(
        f(o).id,
        t,
        {
          ordered: !0,
          ...n
        },
        !1,
        (y, m) => {
          const w = new Ke(m);
          d(w);
        }
      );
    });
    return await c.waitTilReady(), c;
  }
  async sendRealtimeMessage(t, n = {
    channelLabel: "stream.reliable"
  }) {
    const i = f(
      this.rtcClient,
      "Realtime connection has not been started"
    ), o = await this.getRemotePeer();
    i.send(f(o).id, t, n);
  }
  async getRealtimeAudioStreams() {
    var i, o, c;
    const t = await this.getConfiguration(), n = [];
    for (const d of ((i = t.teleop) == null ? void 0 : i.hardwareStreams) ?? [])
      d.rtcStreamType === "audio-chunk" && n.push({
        name: d.name
      });
    for (const d of ((o = t.teleop) == null ? void 0 : o.rosStreams) ?? [])
      d.topicType == "audio_common_msgs/AudioData" && n.push({
        name: d.topicName
      });
    for (const d of ((c = t.teleop) == null ? void 0 : c.customStreams) ?? [])
      d.rtcStreamType === "audio-chunk" && n.push({
        name: d.name
      });
    return n;
  }
}
const Ze = new TextEncoder();
new TextDecoder();
function et(a) {
  const e = JSON.stringify(a), t = Ze.encode(e), n = we(t);
  return pe(n);
}
async function Z() {
  if (!r.token)
    throw new Error("Not authenticated");
  return (await (await fetch(`${h}/v1/admin/views`, {
    method: "GET",
    headers: {
      "Content-Type": "application/json",
      Authorization: "Bearer " + r.token
    }
  })).json()).items;
}
async function ee(a, e) {
  if (!r.token)
    throw new Error("Not authenticated");
  const n = (await Z()).filter((d) => d.name === e);
  if (n.length === 0)
    return console.warn("View does not exist or it is misspell"), null;
  const i = await fetch(`${h}/v1/admin/shares`, {
    method: "POST",
    body: JSON.stringify(a),
    headers: {
      "Content-Type": "application/json",
      Authorization: "Bearer " + r.token
    }
  }), o = h.replace("api", "app"), { code: c } = await i.json();
  return `${o}/shares/${c}#${et({
    viewId: n[0].id
  })}`;
}
const en = [
  "day",
  "week",
  "month",
  "year",
  "hour",
  "minute",
  "quarter"
], tn = [
  "interval",
  "start",
  "end",
  "sub",
  "get"
];
function tt(a) {
  return a.count < 2 ? 0 : a.sumOfSquares / (a.count - 1);
}
function nt(a) {
  return Math.sqrt(tt(a));
}
function at(a) {
  return a.max;
}
function it(a) {
  return a.min;
}
function rt(a) {
  return a.count === 0 ? -1 : a.sum / a.count;
}
function ot(a) {
  return a.sum;
}
function st(a) {
  return a.count;
}
const nn = {
  min: it,
  max: at,
  "standard deviation": nt,
  average: rt,
  sum: ot,
  count: st
}, te = {
  day: {
    interval: u.eachDayOfInterval,
    start: u.startOfDay,
    end: u.endOfDay,
    sub: u.subDays,
    get: u.getDay
  },
  week: {
    interval: u.eachWeekOfInterval,
    start: u.startOfWeek,
    end: u.endOfWeek,
    sub: u.subWeeks,
    get: u.getWeek
  },
  month: {
    interval: u.eachMonthOfInterval,
    start: u.startOfMonth,
    end: u.endOfMonth,
    sub: u.subMonths,
    get: u.getMonth
  },
  year: {
    interval: u.eachYearOfInterval,
    start: u.startOfYear,
    end: u.endOfYear,
    sub: u.subYears,
    get: u.getYear
  },
  hour: {
    interval: u.eachHourOfInterval,
    start: u.startOfHour,
    end: u.endOfHour,
    sub: u.subHours,
    get: u.getHours
  },
  minute: {
    interval: u.eachMinuteOfInterval,
    start: u.startOfMinute,
    end: u.endOfMinute,
    sub: u.subMinutes,
    get: u.getMinutes
  },
  quarter: {
    interval: u.eachQuarterOfInterval,
    start: u.startOfQuarter,
    end: u.endOfQuarter,
    sub: u.subQuarters,
    get: u.getQuarter
  }
}, ct = (a, e) => (
  // FIXME this doesn't work for *a lot* locales, other than en-US
  // en-GB: 'dd/mm/YYYY'
  // ja-jp: 'YYYY/mm/dd'
  // bg-BG: 'dd.mm.YYYY'
  a.split("/")[0] + "/" + a.split("/")[1] + "–" + e.split("/")[0] + "/" + e.split("/")[1]
);
async function z(a) {
  if (!r.token)
    throw new Error("Not authenticated");
  return (await (await fetch(`${h}/v1/admin/events/query`, {
    method: "POST",
    body: JSON.stringify(a),
    headers: {
      "Content-Type": "application/json",
      Authorization: "Bearer " + r.token
    }
  })).json()).items;
}
async function ne(a, e, t, n, i) {
  const o = te[e];
  return await Promise.all(
    Array(t).fill(0).map(async (c, d) => {
      const y = new Date(n), m = o.sub(
        o.start(y),
        t - d - 1
      ), w = o.sub(
        o.end(y),
        t - d - 1
      ), v = ct(
        m.toLocaleDateString(),
        w.toLocaleDateString()
      ), C = await z({
        ...i,
        eventTypes: a,
        start: new Date(m).toISOString(),
        end: new Date(w).toISOString()
      });
      return { date: v, events: C };
    })
  );
}
async function M(a, e) {
  return (await z({
    ...a,
    eventTypes: ["annotation"]
  })).filter(
    (o) => !!o.tags && Object.keys(o.tags).includes(e)
  ).reduce((o, c) => {
    const d = c.tags[e];
    return d in o ? (o[d] += 1, o) : (o[d] = 1, o);
  }, {});
}
async function ae(a, e, t) {
  const { end: n, start: i } = a, c = te[t].interval({
    start: new Date(i),
    end: new Date(n)
  }), d = c.map((m, w) => {
    const v = new Date(m).toISOString(), C = w === c.length - 1 ? new Date(Date.now()).toISOString() : new Date(c[w + 1]);
    return M(
      {
        ...a,
        start: v,
        end: C
      },
      e
    );
  }), y = await Promise.all(d);
  return c.map((m, w) => ({
    date: new Date(m).toISOString(),
    annotations: y[w]
  }));
}
async function ie(a, e, t, n, i) {
  let o = a;
  Array.isArray(a) || (o = [a]);
  let c = e;
  return Array.isArray(e) || (c = [e]), (await (await fetch(`${h}/v1/queries/queries`, {
    method: "POST",
    body: JSON.stringify({
      deviceIds: o,
      end: n.toISOString(),
      names: c,
      start: t.toISOString(),
      tags: i
    }),
    headers: {
      "Content-Type": "application/json",
      Authorization: "Bearer " + r.token
    }
  })).json()).items;
}
async function re() {
  if (!r.token)
    throw new Error("Not authenticated");
  const a = $.get();
  try {
    return await a.getSessions();
  } finally {
    await a.shutdown();
  }
}
async function oe() {
  if (!r.token)
    throw new Error("Not authenticated");
  const a = $.get();
  try {
    return await a.getPeers();
  } finally {
    await a.shutdown();
  }
}
async function dt(a) {
  if (!r.token)
    throw new Error("Not authenticated");
  return await (await fetch(`${h}/v1/admin/devices`, {
    method: "POST",
    body: JSON.stringify(a),
    headers: {
      "Content-Type": "application/json",
      Authorization: "Bearer " + r.token
    }
  })).json();
}
async function ht(a, e) {
  if (!r.token)
    throw new Error("Not authenticated");
  return await (await fetch(`${h}/v1/admin/devices/${a}`, {
    method: "PATCH",
    body: JSON.stringify(e),
    headers: {
      "Content-Type": "application/json",
      Authorization: "Bearer " + r.token
    }
  })).json();
}
async function lt() {
  if (!r.token)
    throw new Error("Not authenticated");
  return (await (await fetch(`${h}/v1/admin/device-details/query`, {
    method: "POST",
    body: JSON.stringify({ enabled: !0, type: "default" }),
    headers: {
      "Content-Type": "application/json",
      Authorization: "Bearer " + r.token
    }
  })).json()).items;
}
async function ut(a) {
  if (!r.token)
    throw new Error("Not authenticated");
  return (await (await fetch(`${h}/v1/admin/devices/query`, {
    method: "POST",
    body: JSON.stringify(a),
    headers: {
      "Content-Type": "application/json",
      Authorization: "Bearer " + r.token
    }
  })).json()).items;
}
async function mt(a) {
  if (!r.token)
    throw new Error("Not authenticated");
  return await (await fetch(
    `${h}/v1/admin/devices/${a}/disable`,
    {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        Authorization: "Bearer " + r.token
      }
    }
  )).json();
}
class S extends Y {
  constructor(e, t, n, i) {
    super(), this.id = e, this.name = t, this.organizationId = n, this.tags = i;
  }
  async getLatestTelemetry() {
    return (await (await fetch(
      `${h}/v1/queries/stream-current-value`,
      {
        method: "POST",
        body: JSON.stringify({
          deviceIds: [this.id]
        }),
        headers: {
          "Content-Type": "application/json",
          Authorization: "Bearer " + r.token
        }
      }
    )).json()).items;
  }
  /**
   * Asynchronously retrieves the configuration document for a device.
   *
   * @param {boolean} getDesiredConfigurationVersion - Whether to retrieve the desired configuration version
   * @returns {Promise<ConfigurationDocument>} A promise that resolves to the configuration document
   * @throws {Error} Throws an error if the device has no configuration or has never been turned on
   */
  async getConfiguration(e = !1) {
    let t = await fetch(`${h}/v1/admin/devices/${this.id}`, {
      method: "GET",
      headers: {
        "Content-Type": "application/json",
        Authorization: "Bearer " + r.token
      }
    });
    const n = await t.json();
    if (!n.state.reportedConfiguration)
      throw new Error(
        "Device has no configuration, has it ever been turned on?"
      );
    const i = e ? n.desiredConfigurationVersion : n.state.reportedConfiguration.version;
    return t = await fetch(
      `${h}/v1/admin/devices/${this.id}/configurations/${i}`,
      {
        method: "GET",
        headers: {
          "Content-Type": "application/json",
          Authorization: "Bearer " + r.token
        }
      }
    ), (await t.json()).document;
  }
  /**
   * Asynchronously retrieves the device's agent version string
   *
   * @returns {Promise<string | undefined | null>} A promise that resolves to the agent version
   * @throws {Error} Throws an error if the device info cannot be fetched
   */
  async getAgentVersion() {
    var n;
    const t = await (await fetch(`${h}/v1/admin/devices/${this.id}`, {
      method: "GET",
      headers: {
        "Content-Type": "application/json",
        Authorization: "Bearer " + r.token
      }
    })).json();
    return (n = t == null ? void 0 : t.state) == null ? void 0 : n.agentVersion;
  }
  async getFileUrl(e) {
    return (await (await fetch(`${h}/v1/admin/files/query`, {
      method: "POST",
      body: JSON.stringify({
        fileIds: [e]
      }),
      headers: {
        "Content-Type": "application/json",
        Authorization: "Bearer " + r.token
      }
    })).json()).fileUrls;
  }
  /**
   * Starts a real-time connection with the remote device using WebRTC.
   * @param {number} [options] - Optional session type to be used for the connection.
   * @throws `Error`  If the connection could not be established or if a connection already exists.
   * @returns {void}
   */
  async startRealtimeConnection(e = {}) {
    if (console.debug(`${(/* @__PURE__ */ new Date()).toISOString()} :: Connection start requested`), this.rtcClient && this.connectionMonitorInterval !== void 0)
      throw new Error(
        `Already created realtime connection to device ${this.id}`
      );
    this.rtcClient && console.warn(
      "overwriting existing rtcClient due to missing connectionMonitorInterval"
    );
    const {
      sessionType: t,
      deadlineMs: n = 1e4,
      maxConnectRetries: i = 3
    } = typeof e == "number" ? { sessionType: e } : e, c = Fe({
      sessionType: t
    }).get(this.handleMessage);
    let d = !1;
    const y = new Promise(
      (w, v) => setTimeout(() => {
        d = !0, v(
          new Error(
            "Connection timed out: the connection could not be finalized in time, possibly due to network issues or misconfigured settings."
          )
        );
      }, n)
    ), m = async () => {
      if ("isReady" in c)
        for (; !c.isReady(); )
          this.assertNotCancelled(d), await b(100);
      const w = await this.getRemoteDevicePeerId(c);
      this.assertNotCancelled(d);
      let v;
      for (let j = 0; j < i && (v = await c.connect(w), !v); j++)
        b(100), this.assertNotCancelled(d);
      if (!v)
        throw new Error(
          `Session could not be created: exhausted ${i} retries`
        );
      let C = 0;
      for (; !d && c.getConnectionStatus(w) !== "connected"; )
        await b(100), C += 1;
      return this.assertNotCancelled(d), console.debug(
        `${(/* @__PURE__ */ new Date()).toISOString()} :: Connection completed after ${C} retries`
      ), w;
    };
    return Promise.race([m(), y]).then((w) => {
      this.remoteDevicePeerId = w, this.initConnectionMonitoring(), this.rtcClient = c, this.emit("connect");
    }).catch((w) => {
      throw console.debug(
        `${(/* @__PURE__ */ new Date()).toISOString()} :: Connection failed: %o`,
        w
      ), this.remoteDevicePeerId = null, c.shutdown().catch((v) => {
        console.error("rtcClient cannot shutdown: %o", v);
      }), this.emit("connection_failed", w), w;
    });
  }
  async getRemoteDevicePeerId(e) {
    const n = (await e.getPeers()).find((i) => i.deviceId === this.id);
    if (!We(n))
      throw new Error("Cannot find peer, is the robot offline?");
    return n.id;
  }
  initConnectionMonitoring() {
    this.connectionMonitorInterval = setInterval(async () => {
      let e = !1;
      if (this.rtcClient) {
        const n = this.rtcClient.getConnections().find(
          (i) => i.getRemotePeerId() === this.remoteDevicePeerId && i.isActive()
        );
        (n === void 0 || !n.isReady()) && (console.debug(`${(/* @__PURE__ */ new Date()).toISOString()} :: data channel closed`), e = !0);
      }
      (!this.rtcClient || !this.remoteDevicePeerId || await this.rtcClient.getConnectionStatsInfo(
        this.remoteDevicePeerId
      ) === void 0 || e) && (this.emit("disconnect"), this.stopRealtimeConnection().catch((t) => {
        console.error(t);
      }));
    }, 1e3);
  }
  async getRemotePeer() {
    const t = (await f(
      this.rtcClient,
      "Realtime connection has not been started"
    ).getPeers()).find((n) => n.deviceId === this.id);
    return f(
      t,
      "Could not find remote peer for device " + this.id
    );
  }
  async stopRealtimeConnection() {
    let e = !1;
    if (this.rtcClient) {
      this.stopConnectionMonitoring(), this.remoteDevicePeerId ? (await this.rtcClient.disconnect(this.remoteDevicePeerId), this.remoteDevicePeerId = null) : e = !0;
      try {
        await this.rtcClient.shutdown();
      } finally {
        this.rtcClient = void 0;
      }
    }
    if (e)
      throw new Error(`Realtime connection hasn't been started for ${this.id}`);
  }
  async isInRealtimeSession() {
    const e = await oe(), t = await re(), n = e.find((i) => i.deviceId === this.id);
    return n ? t[n.id].length > 0 : !1;
  }
  async getAvailableCommands() {
    return (await (await fetch(
      `${h}/v1/admin/command-templates/`,
      {
        method: "GET",
        headers: {
          "Content-Type": "application/json",
          Authorization: "Bearer " + r.token
        }
      }
    )).json()).items.map((n) => ({
      name: n.name,
      id: n.id,
      command: n.command,
      description: n.description,
      parameterEnabled: n.parameterEnabled,
      parameterValue: n.parameterValue,
      parameterMeta: n.parameterMeta,
      enabled: n.enabled,
      tags: n.tags
    }));
  }
  async sendCommand(e, t, n, i) {
    var w;
    const c = (await this.getAvailableCommands()).find((v) => v.name === e);
    if (!c)
      throw new Error(`Could not find command with name "${e}"`);
    let d = "";
    t === void 0 ? c.parameterEnabled && c.parameterValue && (d = c.parameterValue) : d = t;
    let y = {
      value: d,
      scrubberTime: (n || /* @__PURE__ */ new Date()).toISOString(),
      meta: {
        ...c.parameterMeta,
        ...i
      }
    };
    return await fetch(`${h}/v1/admin/commands`, {
      method: "POST",
      body: JSON.stringify({
        commandTemplateId: c.id,
        organizationId: this.organizationId,
        deviceId: this.id,
        command: c.command,
        parameter: y,
        userId: (w = r.currentUser) == null ? void 0 : w.id
      }),
      headers: {
        "Content-Type": "application/json",
        Authorization: "Bearer " + r.token
      }
    });
  }
  async getCommand(e) {
    return await fetch(`${h}/v1/admin/commands/${e}`, {
      method: "GET",
      headers: {
        "Content-Type": "application/json",
        Authorization: "Bearer " + r.token
      }
    });
  }
  async createCaptureStream(e) {
    const n = await (await fetch(`${h}/v1/admin/capture-sessions`, {
      method: "POST",
      body: JSON.stringify({
        deviceId: this.id,
        streamName: e,
        tags: {}
      }),
      headers: {
        "Content-Type": "application/json",
        Authorization: "Bearer " + r.token
      }
    })).json();
    return new He(n);
  }
  async getTelemetry(e, t, n, i) {
    return await ie(
      this.id,
      e,
      t,
      n,
      i
    );
  }
  async getTelemetryStreams() {
    var d, y;
    const e = await this.getConfiguration(), t = await fetch(
      `${h}/v1/queries/metadata/stream-names`,
      {
        method: "POST",
        body: JSON.stringify({
          deviceIds: [this.id]
        }),
        headers: {
          "Content-Type": "application/json",
          Authorization: "Bearer " + r.token
        }
      }
    ), n = [], i = [];
    return (y = (d = e.telemetry) == null ? void 0 : d.streams) == null || y.forEach((m) => {
      m.disabled !== !0 && n.push(m.name), m.onDemand === !0 && i.push(m.name);
    }), console.log(i), (await t.json()).items.filter((m) => !n.includes(m)).map((m) => ({ name: m, onDemand: i.includes(m) }));
  }
  async createInterventionRequest(e, t, n, i) {
    return await (await fetch(
      `${h}/v1/admin/intervention-requests`,
      {
        method: "POST",
        body: JSON.stringify({
          message: e,
          interventionType: t,
          time: (/* @__PURE__ */ new Date()).toISOString(),
          deviceId: this.id,
          tags: i,
          data: n
        }),
        headers: {
          "Content-Type": "application/json",
          Authorization: "Bearer " + r.token
        }
      }
    )).json();
  }
  async addInterventionResponse(e, t, n) {
    return await (await fetch(
      `${h}/v1/admin/intervention-responses`,
      {
        method: "POST",
        body: JSON.stringify({
          interventionId: e,
          interventionType: t,
          data: n
        }),
        headers: {
          "Content-Type": "application/json",
          Authorization: "Bearer " + r.token
        }
      }
    )).json();
  }
  async getAnnotationCount(e, t) {
    return await M({ ...e, deviceIds: [this.id] }, t);
  }
  async getAnnotationCountByIntervals(e, t, n) {
    return await ae(
      { ...e, deviceIds: [this.id] },
      t,
      n
    );
  }
  async eventsCounter(e, t, n, i, o) {
    return await ne(e, t, n, i, {
      ...o,
      deviceIds: [this.id]
    });
  }
  async createShareLink(e, t) {
    return e.scope.deviceIds = [this.id], await ee(e, t);
  }
}
s(S, "createDevice", dt), s(S, "patchDevice", ht), s(S, "getDevicesData", lt), s(S, "queryDevicesData", ut), s(S, "disableDevice", mt);
class ft extends Y {
  constructor(t) {
    super();
    s(this, "id");
    s(this, "telemetryStreamActive", !1);
    s(this, "streamTelemetry", {});
    this.peerUrl = t;
  }
  async getLatestTelemetry() {
    this.telemetryStreamActive || this.subscribeToTelemetry();
    const t = this.streamTelemetry;
    return Object.entries(t).map(([i, o]) => ({
      deviceId: this.id,
      streamName: i,
      streamType: "json",
      currentValue: o,
      currentValueTime: o.timestamp,
      tags: {}
    }));
  }
  subscribeToTelemetry() {
    this.telemetryStreamActive = !0;
    let t = 0;
    const n = new XMLHttpRequest();
    n.responseType = "text", n.addEventListener("error", (i) => {
      this.handleXHRError("error");
    }), n.addEventListener("abort", (i) => {
      this.handleXHRError("abort");
    }), n.addEventListener("timeout", (i) => {
      this.handleXHRError("timeout");
    }), n.addEventListener("readystatechange", (i) => {
      n.readyState === XMLHttpRequest.DONE && this.handleXHRError("closed");
    }), n.addEventListener("progress", (i) => {
      const o = i.loaded, c = o - t;
      t = o, n.responseText.substr(-c).split(`
`).forEach((m) => {
        var w;
        if (m.length > 0) {
          const v = JSON.parse(m);
          if ((w = v.result) != null && w.datapoint) {
            const C = v.result.datapoint, j = C.stream;
            delete C.stream, this.streamTelemetry[j] = C;
          }
        }
      });
    }), n.open("POST", `${this.peerUrl}/v1/telemetry`), n.send();
  }
  handleXHRError(t) {
    console.warn(`Telemetry stream ended: ${t}`), this.telemetryStreamActive = !1;
  }
  async getDeviceId() {
    return (await (await fetch(`${this.peerUrl}/v1/config`)).json()).configuration.id;
  }
  async getConfiguration() {
    return (await (await fetch(`${this.peerUrl}/v1/config`)).json()).configuration.document;
  }
  async startRealtimeConnection(t) {
    if (console.debug(`${(/* @__PURE__ */ new Date()).toISOString()} :: Connection start requested`), this.rtcClient && this.connectionMonitorInterval !== void 0)
      throw new Error(
        `Already created realtime connection to device ${this.id}`
      );
    this.rtcClient && console.warn(
      "overwriting existing rtcClient due to missing connectionMonitorInterval"
    );
    const n = new A({
      lanOnlyMode: !0,
      receive: this.handleMessage,
      sessionType: t
    });
    for (await n.connectLan(this.peerUrl); n.getConnectionStatus(this.peerUrl) !== "connected"; )
      await b(100);
    this.rtcClient = n, this.initConnectionMonitoring();
  }
  initConnectionMonitoring() {
    this.connectionMonitorInterval = setInterval(async () => {
      let t = !1;
      this.rtcClient && this.rtcClient.getConnectionStatus(this.peerUrl) !== "connected" && (console.debug(`${(/* @__PURE__ */ new Date()).toISOString()} :: data channel closed`), t = !0), (!this.rtcClient || t) && (this.emit("disconnect"), this.stopRealtimeConnection().catch((n) => {
        console.error(n);
      }));
    }, 1e3);
  }
  async getRemotePeer() {
    return {
      id: this.peerUrl,
      organizationId: "",
      deviceId: this.id,
      capabilities: [],
      capabilitySet: {}
    };
  }
  async stopRealtimeConnection() {
    let t = !1;
    if (this.rtcClient) {
      this.stopConnectionMonitoring(), this.id ? (await this.rtcClient.disconnect(this.id), this.remoteDevicePeerId = null) : t = !0;
      try {
        await this.rtcClient.shutdown();
      } finally {
        this.rtcClient = void 0;
      }
    }
    if (t)
      throw new Error(`Realtime connection hasn't been started for ${this.id}`);
  }
  async sendCommand(t, n, i, o) {
    const c = {
      value: n,
      scrubberTime: (i || /* @__PURE__ */ new Date()).toISOString(),
      meta: o
    };
    return await fetch(`${this.peerUrl}/v1/enqueue-command`, {
      method: "POST",
      body: JSON.stringify({
        command: t,
        parameter: c
      }),
      headers: {
        "Content-Type": "application/json"
      }
    });
  }
}
async function wt(a, e) {
  if (!r.token)
    throw new Error("Not authenticated");
  return await (await fetch(`${h}/v1/admin/devices/${a}`, {
    method: "PATCH",
    body: JSON.stringify({ fleetId: e }),
    headers: {
      "Content-Type": "application/json",
      Authorization: "Bearer " + r.token
    }
  })).json();
}
async function pt(a) {
  if (!r.token)
    throw new Error("Not authenticated");
  return (await (await fetch(`${h}/v1/queries/queries`, {
    method: "POST",
    body: JSON.stringify(a),
    headers: {
      "Content-Type": "application/json",
      Authorization: "Bearer " + r.token
    }
  })).json()).aggregates;
}
async function yt(a) {
  if (!r.token)
    throw new Error("Not authenticated");
  await fetch(`${h}/v1/admin/fleets/${a}`, {
    method: "DELETE",
    headers: {
      "Content-Type": "application/json",
      Authorization: "Bearer " + r.token
    }
  });
}
async function gt() {
  if (!r.token)
    throw new Error("Not authenticated");
  return (await (await fetch(
    `${h}/v1/queries/analytics/streams`,
    {
      method: "GET",
      headers: {
        "Content-Type": "application/json",
        Authorization: "Bearer " + r.token
      }
    }
  )).json()).items;
}
async function vt() {
  if (!r.token)
    throw new Error("Not authenticated");
  return (await (await fetch(
    `${h}/v1/admin/analytics-modules`,
    {
      method: "GET",
      headers: {
        "Content-Type": "application/json",
        Authorization: "Bearer " + r.token
      }
    }
  )).json()).items;
}
async function Tt(a) {
  if (!r.token)
    throw new Error("Not authenticated");
  return await (await fetch(`${h}/v1/queries/analytics/rows`, {
    method: "POST",
    body: JSON.stringify(a),
    headers: {
      "Content-Type": "application/json",
      Authorization: "Bearer " + r.token
    }
  })).json();
}
async function se(a) {
  if (!r.token)
    throw new Error("Not authenticated");
  return (await (await fetch(`${h}/v1/admin/devices/query`, {
    method: "POST",
    body: JSON.stringify(a),
    headers: {
      "Content-Type": "application/json",
      Authorization: "Bearer " + r.token
    }
  })).json()).items.map(
    (n) => new S(n.id, n.name, n.organizationId, n.tags)
  );
}
async function Ct() {
  if (!r.token)
    throw new Error("Not authenticated");
  let a = new URLSearchParams("");
  typeof window < "u" && window.location && (a = new URLSearchParams(window.location.search));
  const e = a.get("group");
  if (e === null || e.trim() === "")
    return;
  const t = await fetch(
    `${h}/v1/admin/groups/` + e,
    {
      headers: {
        "Content-Type": "application/json",
        Authorization: "Bearer " + r.token
      }
    }
  ), { tagKey: n, tagValue: i } = await t.json();
  return await se({
    tags: { [n]: [i] },
    enabled: !0,
    type: "default"
  });
}
async function St(a) {
  if (!r.token)
    throw new Error("Not authenticated");
  const t = await (await fetch(`${h}/v1/admin/devices/${a}`, {
    method: "GET",
    headers: {
      "Content-Type": "application/json",
      Authorization: "Bearer " + r.token
    }
  })).json(), n = t.name;
  return new S(a, n, t.organizationId, t.tags);
}
async function B() {
  if (!r.token)
    throw new Error("Not authenticated");
  const e = await (await fetch(`${h}/v1/admin/device-details/query`, {
    method: "POST",
    body: JSON.stringify({ enabled: !0, type: "default" }),
    headers: {
      "Content-Type": "application/json",
      Authorization: "Bearer " + r.token
    }
  })).json();
  return e.items, e.items.map(
    (t) => new S(
      t.id,
      t.name,
      f(r.currentOrganization),
      t.tags
    )
  );
}
async function Et(a) {
  if (!r.token)
    throw new Error("Not authenticated");
  return (await (await fetch(
    `${h}/v1/admin/events/query/id=${a}`,
    {
      method: "GET",
      headers: {
        "Content-Type": "application/json",
        Authorization: "Bearer " + r.token
      }
    }
  )).json()).items;
}
async function kt(a) {
  const t = await (await fetch(`${h}/v1/admin/files/query`, {
    method: "POST",
    body: JSON.stringify({
      fileIds: [a]
    }),
    headers: {
      "Content-Type": "application/json",
      Authorization: "Bearer " + r.token
    }
  })).json();
  if (t.fileUrls.length === 0)
    throw new Error("File not found");
  return t.fileUrls[0];
}
async function Ot(a) {
  if (!r.token)
    throw new Error("Not authenticated");
  return await (await fetch(`${h}/v1/admin/fleets/${a}`, {
    method: "GET",
    headers: {
      "Content-Type": "application/json",
      Authorization: "Bearer " + r.token
    }
  })).json();
}
async function Rt(a) {
  if (!r.token)
    throw new Error("Not authenticated");
  return (await (await fetch(`${h}/v1/admin/fleets/${a}/devices`, {
    method: "GET",
    headers: {
      "Content-Type": "application/json",
      Authorization: "Bearer " + r.token
    }
  })).json()).items;
}
async function At() {
  if (!r.token)
    throw new Error("Not authenticated");
  return (await (await fetch(
    `${h}/v1/admin/intervention-requests`,
    {
      method: "GET",
      headers: {
        "Content-Type": "application/json",
        Authorization: "Bearer " + r.token
      }
    }
  )).json()).items;
}
async function bt(...a) {
  const e = a.flat().filter((i) => !!i);
  return e.length === 0 ? [] : (await (await fetch(
    `${h}/v1/queries/stream-current-value`,
    {
      method: "POST",
      body: JSON.stringify({
        deviceIds: e
      }),
      headers: {
        "Content-Type": "application/json",
        Authorization: "Bearer " + r.token
      }
    }
  )).json()).items;
}
async function jt() {
  if (!r.token)
    throw new Error("Not authenticated");
  const t = (await (await fetch(`${h}/v1/queries/online-devices`, {
    method: "GET",
    headers: {
      "Content-Type": "application/json",
      Authorization: "Bearer " + r.token
    }
  })).json()).items;
  return (await B()).filter((i) => t.includes(i.id));
}
async function Nt() {
  if (!r.token)
    throw new Error("Not authenticated");
  const t = (await (await fetch(`${h}/v1/signaling/peers`, {
    method: "GET",
    headers: {
      "Content-Type": "application/json",
      Authorization: "Bearer " + r.token
    }
  })).json()).items.map(
    (i) => i.deviceId
  );
  return (await B()).filter((i) => t.includes(i.id));
}
async function Dt() {
  if (!r.token)
    throw new Error("Not authenticated");
  return (await (await fetch(`${h}/v1/admin/streams`, {
    method: "GET",
    headers: {
      "Content-Type": "application/json",
      Authorization: "Bearer " + r.token
    }
  })).json()).items.filter(
    (t) => t.enabled
  );
}
async function Pt(a) {
  if (!r.token)
    throw new Error("Not authenticated");
  return await (await fetch(
    `${h}/v1/queries/analytics/task-report-rows`,
    {
      method: "POST",
      body: JSON.stringify(a),
      headers: {
        "Content-Type": "application/json",
        Authorization: "Bearer " + r.token
      }
    }
  )).json();
}
async function Lt() {
  if (!r.token)
    throw new Error("Not authenticated");
  return (await (await fetch(
    `${h}/v1/queries/analytics/task-reports`,
    {
      method: "GET",
      headers: {
        "Content-Type": "application/json",
        Authorization: "Bearer " + r.token
      }
    }
  )).json()).items;
}
async function It() {
  if (!r.token)
    throw new Error("Not authenticated");
  return (await (await fetch(`${h}/v1/admin/fleets`, {
    method: "GET",
    headers: {
      "Content-Type": "application/json",
      Authorization: "Bearer " + r.token
    }
  })).json()).items;
}
async function _t(a, e) {
  if (!r.token)
    throw new Error("Not authenticated");
  return await (await fetch(`${h}/v1/admin/fleets/${a}`, {
    method: "PATCH",
    body: JSON.stringify(e),
    headers: {
      "Content-Type": "application/json",
      Authorization: "Bearer " + r.token
    }
  })).json();
}
async function $t(a) {
  if (!r.token)
    throw new Error("Not authenticated");
  return await (await fetch(
    `${h}/v1/admin/streams/${a.id}`,
    {
      method: "PATCH",
      body: JSON.stringify(a),
      headers: {
        "Content-Type": "application/json",
        Authorization: "Bearer " + r.token
      }
    }
  )).json();
}
async function zt(a) {
  if (!r.token)
    throw new Error("Not authenticated");
  return await (await fetch(`${h}/v1/admin/views/${a.id}`, {
    method: "PATCH",
    body: JSON.stringify(a),
    headers: {
      "Content-Type": "application/json",
      Authorization: "Bearer " + r.token
    }
  })).json();
}
async function Mt(a) {
  if (!r.token)
    throw new Error("Not authenticated");
  return await (await fetch(`${h}/v1/queries/analytics`, {
    method: "POST",
    body: JSON.stringify(a),
    headers: {
      "Content-Type": "application/json",
      Authorization: "Bearer " + r.token
    }
  })).json();
}
async function Bt(a) {
  if (!r.token)
    throw new Error("Not authenticated");
  return await (await fetch(`${h}/v1/admin/fleets`, {
    method: "POST",
    body: JSON.stringify(a),
    headers: {
      "Content-Type": "application/json",
      Authorization: "Bearer " + r.token
    }
  })).json();
}
async function qt() {
  if (!r.token)
    throw new Error("Not authenticated");
  return (await (await fetch(`${h}/v1/admin/event-trigger-groups`, {
    method: "GET",
    headers: {
      "Content-Type": "application/json",
      Authorization: "Bearer " + r.token
    }
  })).json()).items;
}
async function xt(a) {
  if (!r.token)
    throw new Error("Not authenticated");
  return await (await fetch(
    `${h}/v1/admin/event-trigger-groups/${a}`,
    {
      method: "GET",
      headers: {
        "Content-Type": "application/json",
        Authorization: "Bearer " + r.token
      }
    }
  )).json();
}
async function Ut(a, e) {
  if (!r.token)
    throw new Error("Not authenticated");
  return await (await fetch(
    `${h}/v1/admin/event-trigger-groups/${a}`,
    {
      method: "PATCH",
      body: JSON.stringify(e),
      headers: {
        "Content-Type": "application/json",
        Authorization: "Bearer " + r.token
      }
    }
  )).json();
}
const E = class {
  static async setDefaultDevice(e) {
    E.defaultDeviceId = e;
  }
  static async getCurrentDevice() {
    if (!r.token)
      throw new Error("Not authenticated");
    if (!E.defaultDeviceId)
      throw new Error("No known default device");
    const n = (await (await fetch(
      `${h}/v1/admin/device-details/query`,
      {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
          Authorization: "Bearer " + r.token
        }
      }
    )).json()).items.find(
      (c) => c.id === E.defaultDeviceId
    ), i = n.name, o = new S(
      E.defaultDeviceId,
      i,
      f(r.currentOrganization),
      n.tags
    );
    return E.knownContext.push(new WeakRef(o)), o;
  }
  static async getPeerDevice(e) {
    const t = new ft(e);
    return t.id = await t.getDeviceId(), t;
  }
  static async getDevice(e) {
    const t = await St(e);
    return E.knownContext.push(new WeakRef(t)), t;
  }
};
let l = E;
s(l, "defaultDeviceId"), s(l, "knownContext", []), s(l, "createFleet", Bt), s(l, "listFleets", It), s(l, "getFleet", Ot), s(l, "patchFleet", _t), s(l, "deleteFleet", yt), s(l, "addDeviceToFleet", wt), s(l, "getFleetDevices", Rt), s(l, "aggregateTelemetry", pt), s(l, "createShareLink", ee), s(l, "eventsCounter", ne), s(l, "getAnalyticStreams", gt), s(l, "getAnalyticsModules", vt), s(l, "getAnalyticsRows", Tt), s(l, "getAnnotationCount", M), s(l, "getAnnotationCountByIntervals", ae), s(l, "getCurrentGroup", Ct), s(l, "getDevices", B), s(l, "getEvent", Et), s(l, "getFileUrl", kt), s(l, "getInterventions", At), s(l, "getLatestTelemetry", bt), s(l, "getOnlineDevices", jt), s(l, "getPeers", oe), s(l, "getRealtimeDevices", Nt), s(l, "getRealtimeSessions", re), s(l, "getStreams", Dt), s(l, "getTaskReportRows", Pt), s(l, "getTaskReportTables", Lt), s(l, "getTelemetry", ie), s(l, "getViews", Z), s(l, "patchStream", $t), s(l, "patchView", zt), s(l, "queryAnalytics", Mt), s(l, "queryDevices", se), s(l, "queryEvents", z), s(l, "queryTelemetry", _), s(l, "getAllEventTriggerGroup", qt), s(l, "getEventTriggerGroup", xt), s(l, "patchEventTriggergroup", Ut);
class an {
  static async set(e, t, n) {
    try {
      const i = await fetch(h + "/v1/admin/key-value", {
        method: "POST",
        body: JSON.stringify({
          organizationId: f(r.currentUser).organizationId,
          key: e,
          value: t,
          tags: n
        }),
        headers: {
          "Content-Type": "application/json",
          Authorization: "Bearer " + r.token
        }
      }), o = await i.json();
      if (i.status !== 200)
        throw new Error(o.message);
    } catch (i) {
      throw i;
    }
  }
  static async get(e) {
    try {
      const t = await fetch(
        h + `/v1/admin/key-value/${e}`,
        {
          method: "GET",
          headers: {
            "Content-Type": "application/json",
            Authorization: "Bearer " + r.token
          }
        }
      ), n = await t.json();
      if (t.status !== 200)
        throw new Error(n.message);
      return n.value;
    } catch (t) {
      throw t;
    }
  }
  static async list() {
    try {
      const e = await fetch(h + "/v1/admin/key-value", {
        method: "GET",
        headers: {
          "Content-Type": "application/json",
          Authorization: "Bearer " + r.token
        }
      }), t = await e.json();
      if (e.status !== 200)
        throw new Error(t.message);
      return t.items;
    } catch (e) {
      throw e;
    }
  }
  static async delete(e) {
    try {
      if (!(await fetch(
        h + `/v1/admin/key-value/${e}`,
        {
          method: "DELETE",
          headers: {
            "Content-Type": "application/json",
            Authorization: "Bearer " + r.token
          }
        }
      )).ok)
        throw new Error("Unable to handle request");
      return;
    } catch (t) {
      throw t;
    }
  }
  static async query(e) {
    try {
      const t = await fetch(
        h + "/v1/admin/key-value/query",
        {
          method: "POST",
          body: JSON.stringify({ keys: e }),
          headers: {
            "Content-Type": "application/json",
            Authorization: "Bearer " + r.token
          }
        }
      );
      if (!t.ok)
        throw new Error("Unable to handle request");
      return (await t.json()).items;
    } catch (t) {
      throw t;
    }
  }
}
function Jt(a) {
  return Uint8Array.from(V(a), (e) => e.charCodeAt(0));
}
function G() {
  const { userAgent: a } = navigator;
  return a ? a.includes("Firefox/") ? "Firefox" : a.includes("Edg/") ? "Edge" : a.includes("Chrome/") ? "Chrome" : a.includes("Safari/") ? "Safari" : a.includes("MSIE/") || a.includes("Trident/") ? "IE" : "Other" : "Other";
}
const Gt = "audio-chunk";
class rn {
  constructor(e, t) {
    s(this, "muted", !1);
    s(this, "hasReceivedData", !1);
    s(this, "audioContext");
    s(this, "chunks", []);
    s(this, "isPlaying", !1);
    s(this, "startTime", 0);
    s(this, "lastChunkOffset", 0);
    s(this, "bufferSize", 3);
    s(this, "receive", async (e) => {
      var c;
      const t = (c = e.payload.audioChunk) == null ? void 0 : c.chunk_data;
      if (!t)
        return;
      this.hasReceivedData || (this.hasReceivedData = !0);
      const { audioContext: n, muted: i } = this;
      if (!n || e.header.stream.streamType !== Gt || i !== !1)
        return;
      const o = Jt(t);
      try {
        await n.decodeAudioData(
          o.buffer,
          this.scheduleChunk
        );
      } catch (d) {
        console.warn(
          "Error decoding audio buffer, changing audioWireFormat on agent",
          { error: d }
        ), this.changeAudioWireFormat("wav");
      }
    });
    s(this, "scheduleChunk", (e) => {
      const { audioContext: t } = this;
      if (!t)
        return;
      (this.chunks.length > this.bufferSize || this.isPlaying === !1) && (this.chunks.forEach((i) => {
        i.stop();
      }), this.isPlaying = !1, this.chunks = []);
      const n = this.createChunk(e);
      n && n.buffer && (this.isPlaying === !1 && (this.startTime = t.currentTime, this.lastChunkOffset = 0, this.isPlaying = !0), n.start(this.startTime + this.lastChunkOffset, 0, e.duration), this.lastChunkOffset += n.buffer.duration, this.chunks.push(n));
    });
    this.device = e, this.stream = t, this.device.startListeningToRealtimeDataStream(t), this.device.addRealtimeListener((i, o) => {
      this.receive(o);
    }), G() === "Safari" || G() === "IE" ? this.changeAudioWireFormat("wav") : this.changeAudioWireFormat("opus");
    const n = window.AudioContext || window.webkitAudioContext;
    this.audioContext = new n();
  }
  async play() {
    var e, t;
    ((e = this.audioContext) == null ? void 0 : e.state) === "suspended" && await ((t = this.audioContext) == null ? void 0 : t.resume()), this.muted = !1;
  }
  async pause() {
    await this.audioContext.suspend(), this.muted = !0;
  }
  destroy() {
    this.device.stopListeningToRealtimeDataStream(this.stream);
  }
  createChunk(e) {
    const { audioContext: t } = this;
    if (!t)
      return;
    const n = t.createBufferSource();
    return n.buffer = e, n.connect(t.destination), n.loop = !1, n.onended = (i) => {
      this.chunks.splice(this.chunks.indexOf(n), 1), this.chunks.length === 0 && (this.isPlaying = !1);
    }, n;
  }
  changeAudioWireFormat(e) {
    const { stream: t } = this;
    (async () => await this.device.changeStreamAudioType(t.name, e))();
  }
}
class on {
  static async listAccounts() {
    if (!r.token)
      throw new Error("Not authenticated");
    return (await (await fetch(`${h}/v1/admin/accounts`, {
      method: "GET",
      headers: {
        "Content-Type": "application/json",
        Authorization: "Bearer " + r.token
      }
    })).json()).items;
  }
  static async createAccounts(e) {
    if (!r.token)
      throw new Error("Not authenticated");
    return await (await fetch(`${h}/v1/admin/accounts`, {
      method: "POST",
      body: JSON.stringify(e),
      headers: {
        "Content-Type": "application/json",
        Authorization: "Bearer " + r.token
      }
    })).json();
  }
  static async getAccount(e) {
    if (!r.token)
      throw new Error("Not authenticated");
    return await (await fetch(`${h}/v1/admin/accounts/${e}`, {
      method: "GET",
      headers: {
        "Content-Type": "application/json",
        Authorization: "Bearer " + r.token
      }
    })).json();
  }
  static async patchAccount(e, t) {
    if (!r.token)
      throw new Error("Not authenticated");
    return await (await fetch(`${h}/v1/admin/accounts/${e}`, {
      method: "PATCH",
      body: JSON.stringify(t),
      headers: {
        "Content-Type": "application/json",
        Authorization: "Bearer " + r.token
      }
    })).json();
  }
  static async deleteAccount(e) {
    if (!r.token)
      throw new Error("Not authenticated");
    if (!(await fetch(`${h}/v1/admin/accounts/${e}`, {
      method: "DELETE",
      headers: {
        "Content-Type": "application/json",
        Authorization: "Bearer " + r.token
      }
    })).ok)
      throw new Error("Unable to delete account");
  }
  static async getAccountTree(e) {
    if (!r.token)
      throw new Error("Not authenticated");
    return await (await fetch(
      `${h}/v1/admin/accounts/${e}/tree`,
      {
        method: "GET",
        headers: {
          "Content-Type": "application/json",
          Authorization: "Bearer " + r.token
        }
      }
    )).json();
  }
}
class sn {
  static async listRoles() {
    if (!r.token)
      throw new Error("Not authenticated");
    return (await (await fetch(`${h}/v1/admin/roles`, {
      method: "GET",
      headers: {
        "Content-Type": "application/json",
        Authorization: "Bearer " + r.token
      }
    })).json()).items;
  }
  static async createRole(e) {
    if (!r.token)
      throw new Error("Not authenticated");
    return await (await fetch(`${h}/v1/admin/roles`, {
      method: "POST",
      body: JSON.stringify(e),
      headers: {
        "Content-Type": "application/json",
        Authorization: "Bearer " + r.token
      }
    })).json();
  }
  static async getRole(e) {
    if (!r.token)
      throw new Error("Not authenticated");
    return await (await fetch(`${h}/v1/admin/roles/${e}`, {
      method: "GET",
      headers: {
        "Content-Type": "application/json",
        Authorization: "Bearer " + r.token
      }
    })).json();
  }
  static async patchRole(e, t) {
    if (!r.token)
      throw new Error("Not authenticated");
    return await (await fetch(`${h}/v1/admin/roles/${e}`, {
      method: "PATCH",
      body: JSON.stringify(t),
      headers: {
        "Content-Type": "application/json",
        Authorization: "Bearer " + r.token
      }
    })).json();
  }
  static async deleteRole(e) {
    if (!r.token)
      throw new Error("Not authenticated");
    await fetch(`${h}/v1/admin/roles/${e}`, {
      method: "DELETE",
      headers: {
        "Content-Type": "application/json",
        Authorization: "Bearer " + r.token
      }
    });
  }
}
class cn {
  static async listUsers() {
    if (!r.token)
      throw new Error("Not authenticated");
    return (await (await fetch(`${h}/v1/admin/users`, {
      method: "GET",
      headers: {
        "Content-Type": "application/json",
        Authorization: "Bearer " + r.token
      }
    })).json()).items;
  }
  static async createUser(e) {
    if (!r.token)
      throw new Error("Not authenticated");
    return await (await fetch(`${h}/v1/admin/users`, {
      method: "POST",
      body: JSON.stringify(e),
      headers: {
        "Content-Type": "application/json",
        Authorization: "Bearer " + r.token
      }
    })).json();
  }
  static async getUser(e) {
    if (!r.token)
      throw new Error("Not authenticated");
    return await (await fetch(`${h}/v1/admin/users/${e}`, {
      method: "GET",
      headers: {
        "Content-Type": "application/json",
        Authorization: "Bearer " + r.token
      }
    })).json();
  }
  static async patchUser(e, t) {
    if (!r.token)
      throw new Error("Not authenticated");
    return await (await fetch(`${h}/v1/admin/users/${e}`, {
      method: "PATCH",
      body: JSON.stringify(t),
      headers: {
        "Content-Type": "application/json",
        Authorization: "Bearer " + r.token
      }
    })).json();
  }
  static async deleteUser(e) {
    if (!r.token)
      throw new Error("Not authenticated");
    await fetch(`${h}/v1/admin/users/${e}`, {
      method: "PATCH",
      body: JSON.stringify({ enabled: !1, roleId: null, teamId: null }),
      headers: {
        "Content-Type": "application/json",
        Authorization: "Bearer " + r.token
      }
    });
  }
}
const dn = ["viewer", "operator", "administrator"], hn = "viewer", ln = "operator", un = "administrator", mn = [
  "year",
  "month",
  "week",
  "day",
  "hour",
  "minute"
], fn = ["tag", "sheet", "user"], wn = [
  "triggered-event",
  "intervention-request",
  "teleop-session-record",
  "port-forwarding-session-record",
  "command-request",
  "command-response",
  "command-delivery",
  "custom",
  "comment",
  "system",
  "annotation"
], pn = [
  "unknown",
  "operational",
  "offline",
  "error"
], yn = ["selection", "labeling", "teleop"], gn = ["info", "warning", "error", "critical"], vn = ["video/mp4"], Tn = (a) => new Promise((e) => setTimeout(e, a * 1e3));
try {
  const a = typeof window < "u" && window.location ? new URLSearchParams(window.location.search) : new URLSearchParams(""), e = a.get("device");
  e && l.setDefaultDevice(e);
  const t = a.get("auth");
  t && r.loginWithToken(t), a.get("module") && r.listenForRefresh(), typeof window < "u" && g.listenForConnectionEvents();
} catch {
}
export {
  on as Account,
  g as App,
  rn as AudioPlayer,
  r as Authentication,
  Xe as BinaryRequestDataChannel,
  He as CaptureStream,
  Ke as DataChannel,
  S as Device,
  l as Fleet,
  an as KeyValue,
  Qe as Manipulator,
  ft as PeerDevice,
  sn as Role,
  Zt as SessionType,
  Ye as TextRequestDataChannel,
  cn as User,
  dn as accessLevels,
  un as administrator,
  te as aggregateByDateFunctions,
  nn as aggregateFunctionMap,
  tn as aggregateFunctions,
  mn as aggregateLevels,
  fn as annotationTypes,
  wn as eventTypes,
  ct as formatTimeFrameText,
  rt as getAverage,
  st as getCount,
  at as getMax,
  it as getMin,
  nt as getStandardDeviation,
  ot as getSum,
  tt as getVariance,
  pn as healthStatuses,
  yn as interventionTypes,
  ln as operator,
  gn as severities,
  Tn as timeout,
  en as vailableAggregationIntervals,
  vn as videoMimeTypes,
  hn as viewer
};
//# sourceMappingURL=data-sdk.es.js.map
