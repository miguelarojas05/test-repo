import { Timestamp } from "../../model/Timestamp";
export interface IRtcStreamsInfo {
    items: {
        streamId: string;
        sentCount: number;
        receivedCount: number;
    }[];
    timestamp: Timestamp;
}
