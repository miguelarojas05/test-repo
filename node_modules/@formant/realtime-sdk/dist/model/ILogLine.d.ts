import { IsoDate } from "./IsoDate";
import { LogLevel } from "./LogLevel";
export interface ILogLine {
    timestamp: IsoDate;
    level: LogLevel;
    message: string;
    error?: any;
    meta?: any;
}
